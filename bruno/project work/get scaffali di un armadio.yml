info:
  name: get scaffali di un armadio
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5050/armadi/scaffali
  body:
    type: json
    data: |-
      {
        "armadioId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
