info:
  name: crea tipologia
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:5050/tipologie
  body:
    type: json
    data: |-
      {
        "nome": "Vestiario"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
