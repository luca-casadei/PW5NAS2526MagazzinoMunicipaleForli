info:
  name: get articoli qt basse
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:5050/articoli/carenti
  body:
    type: json
    data: |-
      {
        "qtMinima": 1000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
