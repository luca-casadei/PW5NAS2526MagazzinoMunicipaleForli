info:
  name: crea armadio
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5050/armadi
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
